# 系统提示词

## 名词解释

`system prompt`：给模型的系统级说明。例如你是谁、你能做什么、你应该遵守什么规则；

`组装流水线`：将来自不同地方的不同信息按一定顺序拼接成一份输入；

`动态信息`：经常变化的信息，例如当前日期、当前工作目录等；

## 最小心智模型

```text
core                           核心身份和行为说明
+ tools                        工具列表
+ skills                       skills 元信息
+ memory                       memory 内容
+ claude_md                    CLAUDE.md 指令链
+ dynamic_context              动态环境信息
= final system prompt
```

## 最小实现结构

```python
class SystemPromptBuilder:
    def build(self) -> str:
        parts = []
        parts.append(self._build_core())
        parts.append(self._build_tools())
        parts.append(self._build_skills())
        parts.append(self._build_memory())
        parts.append(self._build_claude_md())
        parts.append(self._build_dynamic())
        return "\n\n".join(p for p in parts if p)
```

每一段只负责一种来源，例如

`_build_tools()` 只负责把工具说明生成出来

`_build_memory()` 只负责拿 memory

`_build_claude_md() `只负责读指令文件
