# Hook系统

不改主循环代码，也能在关键时机插入额外行为。

## 名词解释

`hook`：可以理解为“预留窗口”，即不改变主循环的实际逻辑，可扩展系统功能。

## 最小心智模型

```
主循环继续往前跑
  |
  +-- 到了某个预留时机
  |
  +-- 调用 hook runner
  |
  +-- 收到 hook 返回结果
  |
  +-- 决定继续、阻止、还是补充说明
```

